#!/bin/bash
#Generated by JetBrains Toolbox 3.4.3.81140 at 2026-05-17T17:34:30

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/dean/Applications/DataGrip.app/Contents/MacOS/datagrip" $wait --args "${intellij_args[@]}"